import 'package:flutter/material.dart';
import 'styles.dart';

class MiscList extends StatelessWidget {
  const MiscList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: const []
    );
  }
}